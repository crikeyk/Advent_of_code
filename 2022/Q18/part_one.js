const {input} = require('./input');

console.log(input.length)

const dim = 3

image = tunnel = new Array(dim).fill(0).map(() => new Array(dim).fill(0).map(() => new Array(dim).fill("")));



for(let i=0; i<input.length;i++){

}


console.log(max_coord, min_coord)